[project]
name = "python-experiment-template"
version = "0.1.0"
description = "Python experiment template"
authors = [{name = "Regina Reynolds"}]
requires-python = ">=3.10"

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "cruft",
    "cookiecutter"
]